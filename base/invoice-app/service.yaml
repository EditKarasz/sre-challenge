apiVersion: v1
kind: Service
metadata:
  name: invoice-app-loadbalander
  labels:
    tier: frontend
spec:
  selector:
    app: $APP
    instance: $INSTANCE
  ports:
    - port: 8081
      targetPort: 8081
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: payment-app-endpoint
  labels:
    tier: frontend
spec:
  externalName: payment-provider.payment-provider.svc.cluster.local
  ports:
    - port: 8082
  type: ExternalName
